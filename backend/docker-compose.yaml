
services:
  db:
    image : postgres:18
    container_name: url-shortener-postgres-db
    restart: always
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/
    ports:
      - "5432:5432"

volumes:
  pgdata:
